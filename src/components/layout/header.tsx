import Typography from '../ui/typography';

const Header = () => {
  return (
    <div className="bg-background text-foreground border border-b p-4 text-sm">
      <Typography size={'lg'}>JobSeek</Typography>
    </div>
  );
};

export default Header;
